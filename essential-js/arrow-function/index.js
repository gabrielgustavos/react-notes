function upperName(name) {
  return name.toUpperCase();
}

const lowerName = (name) => name.toUpperCase();

console.log(upperName("gabs without arrow function"));
console.log(lowerName("gabs with arrow function"));
